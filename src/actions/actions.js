export const ADD_TO_CART = (item) => ({
    type: 'ADD_TO_CART',
    item
});

export const REMOVE_FROM_CART = (item) => ({
    type: 'REMOVE_FROM_CART',
    item
});

export const DELETE_FROM_CART = (item) => ({
    type: 'DELETE_FROM_CART',
    item
});

export const REVERSE_FAVORITES = (item) => ({
    type: 'REVERSE_FAVORITES',
    item
});